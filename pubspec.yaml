name: libs_import
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
    analyzer:
    build:
    path:
    source_gen:
    build_runner:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
